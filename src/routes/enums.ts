export enum routes {
  // default layout
  login = 'login',

  // private layout
  dashboard = 'dashboard',
}

export enum routesPrefix {
  public = '/',
  app = '/app',
}

// to ?my-search-param=value url cases
export enum searchParamsRoutes {
  isOpenModal = 'om',
}
