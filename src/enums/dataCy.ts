export enum loginCy {
  userNameInput = 'user-name__input',
  userNameBtn = 'user-name__btn',
}

export enum todoCy {
  goToTodo = 'users-go-to-todos__btn',
  createUser = 'users-create-user__btn',
  createTask = 'create-task__btn',
  deleteBoard = 'delete-board__btn',
  task = 'task__item',
  taskBtn = 'task__btn',
  modalTitle = 'modal-title__input',
  modalDesc = 'modal-desc__input',
  modalStatus = 'modal-status__option',
  modalAssigned = 'modal-assigned__option',
  droppableArea = 'droppable-area__board',
}

export enum baseScreen {
  onEmpty = 'base-screen__on-empty-btn',
  isEmptyMessage = 'base-screen__empty-msg',
}

export enum editableTypo {
  editableInput = 'editable-typo__editable__input',
}

export enum btn {
  float = 'float__btn',
}
