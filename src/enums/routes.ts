export enum routes {
  // public routes
  login = '/login',
  signUp = '/sign-up',

  // private routes
  dashboard = 'dashboard',
}

export enum routesPrefix {
  public = '/',
  app = '/app',
}

// to ?my-search-param=value url cases
export enum searchParamsRoutes {
  isOpenModal = 'om',
}
