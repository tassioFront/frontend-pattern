import styled from 'styled-components';

const Styles = {
  Wrapper: styled.div`
    display: flex;
    flex-direction: column;
    gap: var(--spacing-large);
    align-items: center;
    min-height: 100vh;
    width: 100%;
  `,
};

export default Styles;
